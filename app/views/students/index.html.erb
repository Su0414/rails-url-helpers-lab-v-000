<div>
  <% @students.each do |student| %>
    <div>
      <!-- <%= student.first_name + " " + student.last_name %>
     -->
      <%= link_to("student.first_name", students_path(student.first_name))%>

    </div>
  <% end %>
</div>
