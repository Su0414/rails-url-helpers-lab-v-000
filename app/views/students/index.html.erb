<div>
  <% @students.each do |student| %>
    <div>
      <!-- <%= student.first_name + " " + student.last_name %>
     -->
      <%= student.first_name + " " + student.last_name link_to("student", students_path(student.id))%>

    </div>
  <% end %>
</div>
